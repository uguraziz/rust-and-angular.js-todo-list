[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/runtime-env.js dist/browser && npx -y serve -s dist/browser -l ${PORT:-3000}"